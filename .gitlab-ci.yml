stages:
  - quality

pipeline:
  stage: quality
  script:
    - npm install
    - npm run build-types
    - npm run lint
    - npm run type-check
    - npm run build
  rules:
    - if: $CI_COMMIT_BRANCH
